import { Box } from '@mui/material';
import React from 'react';

interface ChannelDetailProps {}

export const ChannelDetail: React.FC<ChannelDetailProps> = ({}) => {
  return <Box>Channel Detail</Box>;
};
